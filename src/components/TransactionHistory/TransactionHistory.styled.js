import styled from '@emotion/styled';

export const TransTable = styled.table`
  width: 80%;
  box-shadow: 4px 4px 23px 6px rgba(0, 0, 0, 0.3);
  text-align: center;
  margin-left: auto;
  margin-right: auto;
`;

export const TableCell = styled.td`
  padding: 16px;
  font-size: 21px;
  font-weight: 100;
  border: 1px solid grey;
`;

export const TableHeadline = styled.th`
  padding: 16px;
  background-color: rgb(79, 182, 205);
  font-size: 21px;
`;
